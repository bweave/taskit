let $modal = $('#edit-modal'),
    $actionsMenuToClose = $(".list[data-id='<%= @list.id %>'] .header .actions-menu");

$modal.foundation('close');
$actionsMenuToClose.toggleClass('fa-bars').toggleClass('fa-close');
$actionsMenuToClose.siblings('.actions').toggleClass('hide');
