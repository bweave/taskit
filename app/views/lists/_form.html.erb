<%= form_for List.new, url: project_lists_path(project.id), remote: true do |f| %>
    <%= f.hidden_field :project_id, value: project.id %>
    <%= f.text_field :name, placeholder: 'New List...', autocomplete: 'off' %>
<% end %>
